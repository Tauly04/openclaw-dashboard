# 无需信用卡部署指南

## 推荐方案：Koyeb（免费、稳定）

### 一键部署
1. 访问 https://app.koyeb.com
2. 用 GitHub 登录
3. 点击 **Create Web Service** → **GitHub**
4. 选择 `Tauly04/openclaw-dashboard`
5. 填入以下配置：

```
Build Command: pip install -r requirements.txt && cd frontend && npm install && npm run build
Run Command: python server.py
Port: 10000
Instance: Free
```

6. 点击 **Advanced** → **Volumes** → Add Volume:
   - Name: `data`
   - Mount Path: `/data`
   - Size: 1GB

7. 点击 **Deploy**

部署完成后，你会得到一个永久链接如：
`https://openclaw-dashboard-xxx.koyeb.app`

### 数据持久化
- 聊天记录保存在 `/data/openclaw_dashboard.db`
- 换设备登录，数据自动同步

---

## 备选方案：本地保持运行

如果你不想部署到云端，可以在你的 Mac 上保持运行：

```bash
cd openclaw-dashboard
./start.sh
```

这会启动：
- Dashboard 服务 (http://localhost:18790)
- Cloudflare tunnel（自动生成公共链接）

**注意：** Mac 需要保持开机才能访问

---

## 功能说明

### 聊天按钮位置
左侧导航栏 3 个按钮，**第 2 个（气泡图标）** 是聊天：
- 鼠标悬停显示 "💬 点击对话"
- 点击打开聊天窗口
- 聊天记录自动保存

### 跨设备同步
- ✅ 聊天记录（登录后自动加载）
- ✅ 语言设置（中文/英文）
- ✅ 背景图片

### 默认登录
- 用户名：`admin`
- 密码：`admin123`
